spring:
  datasource:
    url: jdbc:postgresql://localhost:16437/postgres

  cloud:
    gcp:
      pubsub:
        emulator-host: localhost:18541
        project-id: our-project

# for wiremock server
exception-manager:
  connection:
    host: localhost
    port: 8667
    scheme: http

queueconfig:
  shared-pubsub-project: shared-project


#MUST BE TRUE FOR PRODUCTION
iap-access-control-enabled: false